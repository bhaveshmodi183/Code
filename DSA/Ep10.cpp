#include<bits/stdc++.h>
using namespace std;
int main()
{
    int x;                   //1 iterator       basically x,sum,n are single iterator
    int sum=0;               //1 iterator       and are called constant time operator 
    sum=x+x;                 //1 iterator       they are taking unit time to execute 
    int n;
    cin>>n;
    for(int i=0;i<n;i++)
    {
        int y;
        y=5;
        y++;
    }
}

// so here time complexity of above programe is O(n).
// number of iterator in one second.